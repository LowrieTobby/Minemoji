name: 'Minemoji' # the plugins name as it should appear in the plugin list /pl
version: '1.0' # the plugin's version
author: 'Lowrie'
main: xyz.lowrie.minemoji.MinemojiPlugin
api-version: '1.13' # the version of the API you want to use, required starting with 1.13